import 'package:flutter/material.dart';

class Result extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text("There is no more questions! You Did It!",
        style: TextStyle(
          fontSize: 22,
          color: Colors.lightGreen,
        ));
  }
}
